This addon removes the background from all report layouts, as the way backgrounds used to be implemented causes a timeout in TOSC's environment.

When Odoo sources are updated to a state after 2025-02, this addon can and should be removed to restore the background functionality.
