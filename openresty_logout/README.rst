Support logout for openresty/keycloak
-------------------------------------

This addon redirects to /logout after an Odoo logout, which triggers a logout prompt from openresty/keycloak.

Note that this relies on the standard logout path not being changed.
