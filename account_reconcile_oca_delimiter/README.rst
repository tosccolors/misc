Custom delimiter in bank statement matching
===========================================

Odoo tokenizes statement line text fields by splitting them on whitespace boundaries.

This addon allows to use additional delimiters for tokenization.
