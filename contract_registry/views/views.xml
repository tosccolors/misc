<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="contract_registry.list">
      <field name="name">contract.registry list</field>
      <field name="model">contract.registry</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>


    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="contract_registry.action_window">
      <field name="name">contract_registry window</field>
      <field name="res_model">contract_registry</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="contract_registry.action_server">
      <field name="name">contract_registry server</field>
      <field name="model_id" ref="model_contract_registry_contract_registry"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="contract_registry" id="contract_registry.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="contract_registry.menu_1" parent="contract_registry.menu_root"/>
    <menuitem name="Menu 2" id="contract_registry.menu_2" parent="contract_registry.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="contract_registry.menu_1_list" parent="contract_registry.menu_1"
              action="contract_registry.action_window"/>
    <menuitem name="Server to list" id="contract_registry" parent="contract_registry.menu_2"
              action="contract_registry.action_server"/>
    -->
  </data>
</odoo>