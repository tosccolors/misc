<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_cancel']" position="after">
                <button string="Send to Monta" class="btn btn-secondary"
                        attrs="{'invisible': [('state', 'in', ('done','cancel'))]}"
                        name="action_send2monta_interface" type="object"/>
            </xpath>
            <xpath expr="//page[last()]" position="after">
                <page name="monta_interface" string="Monta POST Response">
                    <group>

                        <group string="Create Log">
                            <field name="monta_log_id" readonly="True"/>
                            <field name="response_code"/>
                            <field name="response_status"/>
                            <field name="response_message" groups="base.group_no_one"/>
                            <field name="monta_carrier_tracking_url" readonly="True"/>
                        </group>
                        <group string="Update Log">
                            <field name="update_monta_log_id" readonly="True"/>
                            <field name="update_response_code"/>
                            <field name="update_response_status"/>
                            <field name="update_response_message" groups="base.group_no_one"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>